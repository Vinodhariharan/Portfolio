# Claude Code session data
.claude/

# Supabase config is generated at build time from env vars — never commit the real file
supabase-config.js

# Environment / secrets (never commit these)
.env
.env.local
.env*.local

# Dependencies (installed by the Vercel build via npm install)
node_modules/

# OS noise
.DS_Store
Thumbs.db

# Editor
.vscode/
*.swp

# Local UI/design preview files — never push these
previews/
